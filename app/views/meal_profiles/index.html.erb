<h1>Listing meal_profiles</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @meal_profiles.each do |meal_profile| %>
  <tr>
    <td><%= link_to 'Show', meal_profile %></td>
    <td><%= link_to 'Edit', edit_meal_profile_path(meal_profile) %></td>
    <td><%= link_to 'Destroy', meal_profile, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Meal profile', new_meal_profile_path %>
