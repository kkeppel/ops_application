<%= form_for(@meal_profile) do |f| %>
  <% if @meal_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meal_profile.errors.count, "error") %> prohibited this meal_profile from being saved:</h2>

      <ul>
      <% @meal_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
