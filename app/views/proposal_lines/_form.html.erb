<%= form_for(@proposal_line) do |f| %>
  <% if @proposal_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proposal_line.errors.count, "error") %> prohibited this proposal_line from being saved:</h2>

      <ul>
      <% @proposal_line.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
