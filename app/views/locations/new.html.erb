<h2>New location</h2>
<br/>
<%= form_for(@location, :url => params.include?(:company_id) ? company_locations_path(@company) : vendor_locations_path(@vendor), method: "POST") do |f| %>
  <%= render 'form', f: f %>
<% end %>
