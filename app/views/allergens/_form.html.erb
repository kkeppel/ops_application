<%= form_for(@allergen, :url => ingredient_allergens_path(@ingredient)) do |f| %>
  <% if @allergen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@allergen.errors.count, "error") %> prohibited this allergen from being saved:</h2>

      <ul>
      <% @allergen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
