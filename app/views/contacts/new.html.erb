<h1>New contact</h1>

<%= form_for(@contact, :url => params.include?(:company_id) ? company_contacts_path(@company) : vendor_contacts_path(@vendor), method: "POST") do |f| %>
  <%= render 'form', f: f %>
<% end %>
