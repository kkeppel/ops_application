<h1>Edit Company!</h1>
<%= form_for(@company, :url => admin_company_path(@company), method: 'PUT') do |f| %>
	<%= render 'form', f: f %>
<% end %>


<%= link_to 'Show', admin_company_path(@company), :class => 'btn' %> |
<%= link_to 'Back', admin_companies_path, :class => 'btn' %>

