<h1>New Company!</h1>
<%= form_for(@company, :url => admin_companies_path, method: "POST") do |f| %>
	<%= render 'form', f: f %>
<% end %>
<%= link_to 'Back', admin_companies_path, :class => "btn" %>
