<h1>Editing proposal</h1>

<%= form_for(@proposal, :url => order_proposal_path(@order, @proposal), method: "PUT") do |f| %>
	<%= render 'form', f: f %>
<% end %>
<%= link_to 'Back', order_proposals_path(@order) %>
