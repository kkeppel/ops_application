<h1>New proposal</h1>

<%= form_for(@proposal, :url => order_proposals_path(@order, @proposal), method: "POST") do |f| %>
  <%= render 'form', f: f %>
<% end %>

<%= link_to 'Back', order_proposals_path(@order) %>
