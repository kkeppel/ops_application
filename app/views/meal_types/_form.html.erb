<%= form_for(@meal_type) do |f| %>
  <% if @meal_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meal_type.errors.count, "error") %> prohibited this meal_type from being saved:</h2>

      <ul>
      <% @meal_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :meal_type %><br />
      <%= f.text_field :meal_type %>
    </div>

    <div class="field">
      <%= f.label :description %><br />
      <%= f.text_area :description%>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
